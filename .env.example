# Copy this file to .env and fill in your values.
# Never commit .env to version control.

# --- LLM Providers ---
OPENAI_API_KEY=sk-...
# OLLAMA_BASE_URL=http://localhost:11434   # Docker phase1-demo uses http://ollama:11434
# USE_COLPALI=false          # set true + --colpali to use ColPali for page_chunks
# COLPALI_MODEL=vidore/colqwen2-v1.0   # or vidore/colSmol-500M for CPU
# HF_TOKEN=hf_...            # optional — faster Hugging Face model downloads
# USE_HYBRID=false           # BM25 + dense for text_chunks (Phase 2)
# USE_RECURSIVE_CHUNKER=false
# USE_SEMANTIC_CHUNKER=false # alternative to recursive chunking (Phase 2)
# USE_SECTION_PATHS=true
# USE_CONTEXT_ENRICHMENT=true
# USE_PARENT_EXPAND=true     # expand child hits to parent chunks (Phase 2)
# USE_FLASHRANK=false        # lightweight reranker (Phase 2)
# USE_OCR=true             # Tesseract OCR for image-only PDF pages at ingest
# OCR_LANG=eng

# --- Vector DB ---
# QDRANT_API_KEY=          # only needed for Qdrant Cloud; leave blank for local Docker

# --- Embedding device ---
# EMBEDDING_DEVICE=cuda    # set to "cuda" if you have a GPU

# --- Optional overrides (defaults are in shared/config.py) ---
# OPENAI_MODEL=gpt-4o
# TEXT_EMBEDDING_MODEL=BAAI/bge-m3
# QDRANT_URL=http://localhost:6333
# REDIS_URL=redis://localhost:6379
# LOG_LEVEL=INFO
